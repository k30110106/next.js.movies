import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "sales",
};

const page = () => {
  return (
    <div>
      <h1>sales team</h1>
    </div>
  );
};

export default page;
